sudo aptitude install git
sudo aptitude install postgresql
cat software_architecture_course/sql/create_db_and_user.sql | sudo -u postgres psql
ln -s software_architecture_course/.pg_service.conf ~/.pg_service.conf
ln -s software_architecture_course/.pgpass ~/.pgpass
chmod 600 software_architecture_course/.pgpass
cat software_architecture_course/sql/create_table.sql | psql 'service=stat'

local   all             all                                     peer
в /etc/postgresql/9.1/main/pg_hba.conf заменяем на
local   all             all                                     md5

/etc/postgresql/9.1/main/environment
добавляем строку
LANG = 'ru_RU.UTF-8'

sudo service postgresql restart

sudo aptitude install cpanminus
sudo aptitude install libdata-uuid-perl
sudo aptitude install libdbi-perl
sudo aptitude install libdbd-pg-perl

sudo aptitude install uwsgi uwsgi-plugin-psgi

du -s -b uwsgi-stat-example/
perl -i -pe 's/Installed-Size: \d+/Installed-Size: 37906/g' uwsgi-stat-example/DEBIAN/control
find opt/ etc/ -type f -exec md5sum {} ";" > DEBIAN/md5sums
dpkg-deb -b uwsgi-stat-example/

sudo dpkg -i uwsgi-stat-example.deb
#sudo dpkg --purge uwsgi-stat-example

sudo aptitude install curl

sudo aptitude install libipc-run3-perl
